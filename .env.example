# Server
NODE_ENV=development
PORT=3000
LOG_LEVEL=info
TRUST_PROXY=true

# Asgardeo resume endpoint template. Must contain {flowId}. Optionally include {tenant}.
ASGARDEO_RESUME_URL_TEMPLATE=https://api.asgardeo.io/t/{tenant}/commonauth?flowId={flowId}

# Inbound request auth from Asgardeo (basic auth required).
# Asgardeo sends Basic Auth where:
#   username = the Authsignal API secret (per-tenant)
#   password = a shared secret to authenticate Asgardeo requests to the adapter
ASGARDEO_AUTH_MODE=basic
ASGARDEO_BASIC_PASSWORD=shared-password-between-asgardeo-and-adapter

AUTHSIGNAL_API_URL=https://api.authsignal.com

# Flow persistence: memory | redis
STORE_DRIVER=memory
FLOW_TTL_SECONDS=900
REDIS_URL=redis://localhost:6379
